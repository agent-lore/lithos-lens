{
  "name": "lithos-lens-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright smoke suite driving Lithos Lens in fake-Lithos app mode.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "install-browsers": "playwright install --with-deps chromium || playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0"
  }
}
